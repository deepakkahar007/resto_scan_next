# resto scan

create a new component inside the component folder, and the component basically have the only design part, use the shadcnui card and installed component to display orders and order details in a single page, so currently i have a side bar navigation and main content, so i need single component that is divided in to 40, 60 percentage and in that 40 % screen show the orders, like order id, amount, created at, status, and in 60 % screen show the complete order details, like order id, amount, created at, status, paid by, items, gst, and all the nessary details, and also make sure to use the simple design as shadcnui provided just when i click the order card it will only show that specific order details nothing else, and initialy when no card is selected show a empty space, but when the user select the order card from 40% show the order details in the 60% screen card. make sure only design the component in a single file. and only design use no working or anything and use the dummy data of restaurent order and food items.
